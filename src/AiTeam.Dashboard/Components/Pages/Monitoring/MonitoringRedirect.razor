@* Stage 87 B0：/monitoring 入口 redirect 到 /monitoring/tokens（對齊既有 AgentsRedirect / RulesRedirect pattern）*@
@page "/monitoring"
@attribute [Authorize]
@inject NavigationManager Nav

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/monitoring/tokens", forceLoad: false);
}
