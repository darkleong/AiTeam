@page "/settings/rules"
@attribute [Authorize]
@rendermode @(new InteractiveServerRenderMode(prerender: false))
@using AiTeam.Dashboard.Components.Pages.Rules

@* Stage 87 follow-up #10：Rules 跟 Projects 拆兩頁（既有 SettingsRules.razor reuse 兩 component 同頁 MudExpansionPanel）*@

<PageTitle>Rules - AI Team Dashboard</PageTitle>

<div class="page-header">
    <h2>Rules</h2>
</div>

<div class="page-body">
<RuleManagement />
</div>
