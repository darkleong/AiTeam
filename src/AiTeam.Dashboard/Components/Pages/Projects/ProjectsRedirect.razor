@page "/projects"
@attribute [Authorize]
@inject NavigationManager Nav

@* Stage 83 子項 5：/projects redirect → /settings（SettingsHub Rules + Projects tab reuse ProjectManagement component） *@
@code {
    protected override void OnInitialized() => Nav.NavigateTo("/settings", forceLoad: false);
}
