@page "/office"
@attribute [Authorize]
@inject NavigationManager Nav

@* Stage 83 子項 5：Team Office 18 行 placeholder 砍（決策 #4）— redirect 到 Home *@
@code {
    protected override void OnInitialized() => Nav.NavigateTo("/", forceLoad: false);
}
