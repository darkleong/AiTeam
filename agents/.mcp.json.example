{
  "mcpServers": {
    "aiteam-records": {
      "type": "http",
      "url": "${AITEAM_MCP_URL:-http://localhost:5050/mcp}",
      "headers": {
        "Authorization": "Bearer ${AITEAM_MCP_API_KEY}"
      },
      "alwaysLoad": true,
      "timeout": 30000
    }
  }
}
