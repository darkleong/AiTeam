在開始產出規格文件前，我需要先確認幾個任務範圍與目標的問題，避免做超出範圍或方向錯誤的設計。

---

## 確認問題

### 關於顯示載體

1. **報告輸出在哪裡呈現？** 這份 Code Review 報告目前是顯示在某個 Blazor 頁面上（例如 MudDataGrid 的展開列、對話框、詳情頁面），還是純粹以文字/Markdown 格式輸出（例如串流輸出到 `<textarea>` 或 chat-like 介面）？

2. **統計行是「靜態文字」還是「獨立 UI 元件」？** 需求描述的格式「共發現 N 個 🔴 嚴重問題、M 個 🟡 建議」，是直接附加在報告文字末尾的一行純文字，還是希望用 MudBlazor 的元件（例如 MudChip、MudAlert、MudText）獨立渲染成視覺區塊？

### 關於統計邏輯

3. **統計時機點是什麼？** 是在 Reviewer Agent 產出報告完成後（全文完成）才統計，還是需要在串流輸出過程中即時累計？

4. **🔴 與 🟡 的判斷依據是什麼？** 是直接計算報告文字內 emoji 字元的出現次數，還是有結構化資料（例如問題清單的 severity 欄位）可以作為統計來源？

### 關於現有架構

5. **Reviewer Agent（Vera）的輸出目前是怎麼儲存的？** 是存入資料庫的某個欄位（例如 `ReportContent: string`），還是即時串流不落地？這會影響統計行是在後端計算後寫入，還是在前端渲染時動態計算。

---

請 Christ 確認以上問題後，我再產出完整規格文件。🙏